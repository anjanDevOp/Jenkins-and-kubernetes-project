apiVersion: v1
kind: Pod
metadata:
  name: pavanpod
  labels:
    app: proxy
spec:
  containers:
  - name: pavancontainer
    image: anjandevop/maven-standalone-application
    ports:
      - containerPort: 8080
    tolerations:
     - key: "node-role.kubernetes.io/master"
       operator: "Exists"
       effect: "NoSchedule"
         

---
apiVersion: v1
kind: Service
metadata:
  name: pavan-service
spec:
  selector:
    app: proxy
  ports:
   - port: 80
     targetPort: 8080
  type: NodePort
  
